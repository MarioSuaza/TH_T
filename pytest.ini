[pytest]
testpaths = tests
pythonpath = .
addopts = -q --strict-markers
markers =
    realdata: usa los archivos parquet reales de data/raw (desactivar con -m "not realdata")
filterwarnings =
    ignore::DeprecationWarning
